# Class Diagram: {{PROJECT_NAME}}

## Domain Model

```mermaid
classDiagram
    class {{ClassName}} {
        +{{property}}: {{type}}
        -{{privateProperty}}: {{type}}
        +{{method}}({{params}}): {{returnType}}
    }

    class {{InterfaceName}} {
        <<interface>>
        +{{method}}(): {{returnType}}
    }

    {{ClassName}} ..|> {{InterfaceName}} : implements
    {{ClassName}} --> {{OtherClass}} : uses
```

## Component Structure

```mermaid
classDiagram
    class PageComponent {
        +props: PageProps
        +render(): JSX
    }

    class FeatureComponent {
        +props: FeatureProps
        +state: FeatureState
        +handleAction(): void
        +render(): JSX
    }

    class Hook {
        <<hook>>
        +useFeature(): FeatureReturn
    }

    class Service {
        <<service>>
        +fetchData(): Promise~Data~
        +submitData(data): Promise~Result~
    }

    PageComponent --> FeatureComponent : renders
    FeatureComponent --> Hook : uses
    Hook --> Service : calls
```

## Type Definitions

```typescript
// Key interfaces and types referenced in the class diagram
interface {{TypeName}} {
  {{property}}: {{type}};
}
```

---
*Generated by Weave Architect agent.*
