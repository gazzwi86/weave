# QA cross-task findings

Findings discovered during one task's QA pass that affect work in other (not-yet-QA'd or
already-delivered) tasks. Subsequent QA passes for any task listed in `affects` MUST read
this file before validating.

| Raised in | Finding | Severity | Affects | Note |
|---|---|---|---|---|
| PLAT-TASK-002 | No `lighthouserc.json` (or equivalent CI config) pins the canonical Lighthouse methodology (mobile-simulated-throttling vs. desktop preset, `numberOfRuns`). Measured on `/`: default preset scores performance 98/a11y 100/best-practices 100/SEO 100 (LCP 2.5s trips simulated 4G); `--preset=desktop` scores 100/100/100/100 on the identical build. "100 across all four categories" (Law #2 / Category 15) is ambiguous without a pinned preset. | Warn | [PLAT-TASK-003, PLAT-TASK-004] | Owner: Architect — commit a `lighthouserc.json` (or note in `testing-strategy.md`) choosing the preset/throttling method before the next Lighthouse-gated QA pass, so pass/fail isn't preset-dependent. |
| PLAT-EPIC-000 PR review | `verify_access_token` fetches the full JWKS from the issuer on every authenticated request via a fresh `httpx.AsyncClient` (no key cache, no shared client). Harmless against the local mock; a real latency/availability coupling once Cognito is live. | Warn | [PLAT-TASK-004] | Owner: Engineer — add JWKS caching (e.g. PyJWKClient or TTL cache) + module-level shared client when RBAC work touches `auth/`; PLAT-TASK-004 builds directly on `get_current_principal`. |
| PLAT-EPIC-000 PR review | Frontend rate-limit key comes from client-supplied `x-forwarded-for` with no trusted-proxy validation — spoofable per request, bypassing the limiter. Backend/frontend stores also never evict dead keys. | Warn | [PLAT-TASK-004] | Owner: Engineer — when rate limiting is productionised (Redis/slowapi per existing ponytail markers), derive the key behind a trusted-proxy config and add eviction. |
| PLAT-EPIC-000 PR review | `mock_oidc` ships inside the production `weave_backend` package with an unconditional `weave-mock-oidc` entry point — prod artifacts would contain a token-minting IdP. | Blocker-before-deploy | [PLAT-TASK-009, first-deploy checklist] | Owner: Engineer — move mock_oidc to a dev-only distribution (dependency-group/extra or separate package) before any artifact is built for a real environment. |
| PLAT-EPIC-000 PR review | ui_verify step B for URL targets runs the app's own Playwright suite, which currently has zero `toHaveScreenshot` visual baselines — the advertised 8-state visual diff never exercises real screens. | Warn | [PLAT-TASK-005] | Owner: Engineer — TASK-005 (nav/dashboard screens) must add per-feature Playwright visual baselines (8-state per testing-ts.md) so the gate's visual half becomes real. |
| PLAT-EPIC-000 PR review | `add_tenant_attributes` reads `WEAVE_TESTING` env directly (second test-mode mechanism besides `setup_tracing(testing=)`); env leak into a real process turns missing tenant context into request 500s. | Warn | [PLAT-TASK-004] | Owner: Engineer — thread one strict/test flag through setup_tracing instead of the env read when auth/observability is next touched. |
| PLAT-EPIC-000 PR review | playwright.config.ts starts all three local webServers unconditionally even when TEST_BASE_URL targets a remote URL; s3_state/s3_assets/s3_spa terraform modules are a 3-way copy already diverging; JWT-decode logic hand-rolled 3x in mock-oidc tests instead of exercising `verify_access_token`. | Info | [PLAT-TASK-005, v1-infra] | Consolidate opportunistically when the owning files are next touched — not worth standalone churn now. |
| PLAT-TASK-002 | Design token taxonomy is incomplete: `typography.md` defines 9 type-scale tokens (`--text-display`, `--text-h1`..`--text-h4`, `--text-body-lg`, `--text-body-sm`, `--text-overline`, `--text-mono`/`-sm`); `app/globals.css` only implements 3 (`--text-body`, `--text-label`, `--text-caption`). Any task adding a heading/page title has no token to reach for and will fall back to raw Tailwind classes (already happened in `app/page.tsx`'s `<h1>`, not caught by AC-6 since that AC only scoped Button/Input/Badge/Card). | Warn | [PLAT-TASK-003, PLAT-TASK-004] | Owner: Engineer — extend `globals.css` with the missing type-scale tokens (and fix `app/page.tsx`) in the next frontend task that touches typography, before more pages compound the gap. |
| PLAT-EPIC-003 PR review | Settings routes check tenant scope only — any authenticated tenant member can read/write settings for workspaces they were never invited to (within-tenant, cross-workspace). Workspace-membership authorization does not exist yet by design. | CLOSED | [PLAT-TASK-004] | Owner: Engineer — RESOLVED by PLAT-TASK-004 commit 5bf7d04 (see FAIL-1 row below for verification detail). |
| PLAT-TASK-004 QA | FAIL-1 (AC-3): this task's own QA pass found the above finding only partially closed — `invite_member_route`/`revoke_member_route` now use `require_workspace_role("admin")`, but `get_setting_route`/`set_setting_route`/`run_sparql_route`/`switch_workspace_route` still use bare `get_current_principal` (tenant identity only, no membership/role check). Live-proven via new test `test_non_member_can_reach_workspace_settings_and_switch` (`tests/integration/test_identity_rbac.py`, `xfail(strict=True)`): a tenant member with zero `workspace_members` row for the target workspace gets 200 on both routes. `rbac.assert_all_routes_guarded` only proves authentication-by-default, not role-check-by-default, so this gap is structurally invisible to the startup guard. | RESOLVED (commit 5bf7d04) | [PLAT-TASK-004, PLAT-TASK-005] | Owner: Engineer — fix verified by QA re-validation 2026-07-04: `enforce_workspace_role` (rbac.py) + `workspace_of` (settings/scope.py) added; all 4 named routes (settings GET+PUT, sparql, switch) now gate on workspace role. QA added 4 new probe tests (`test_non_member_forbidden_on_settings_write_and_sparql`, `test_read_role_member_cannot_write_settings_author_ceiling`, `test_workspace_role_enforced_against_the_requested_workspace_not_another`, plus the un-xfailed original) proving: zero-membership → 403 on all 4 routes; read-role member → 403 on author-ceiling PUT, author-role member → 200; a member of workspace A with a role there → 403 on workspace B (cross-workspace probe, proves the guard resolves the *requested* workspace, never a cached/active one). All green, docker suite 25 passed. No longer blocks PLAT-TASK-005. |
| PLAT-TASK-004 QA | RESOLVED — FAIL-2: ADR-007 was cited in code + commit but the file was missing. Coordinator authored `docs/specs/weave/engines/weave-platform/decisions/ADR-007.md` (OKF frontmatter matching ADR-005/006; captures the settings-write author-ceiling + the four per-route ceilings and the workspace-derivation rationale). `okf-validate docs` → conformant. Dangling citation closed. | Resolved | [PLAT-TASK-004] | Owner: none — fix accepted. |
| PLAT-TASK-004 QA | Warn (new): `test_local_stack.py::test_local_stack_boots` owns a function-scoped `running_stack` fixture with its own `docker compose down -v` in `finally`. If run in the same pytest invocation as the session-scoped `platform_stack` fixture (used by `test_identity_rbac.py`/`test_tenancy_isolation.py`/`test_workspace_switch_e2e.py`) *without* the `stack` marker excluded, its teardown tears down the shared containers mid-session, breaking every subsequent `platform_stack`-dependent test. Reproduced live: `-m "integration and docker"` (no `and not stack`) → 12 failures, all in files alphabetically after `test_local_stack.py`; `-m "integration and docker and not stack"` (the mitigation already in place via `pytest.mark.stack` on that file) → 25 passed, 0 failures. `test_dev_stack_healthy.py`'s own `running_services` fixture is NOT implicated — it sorts alphabetically before `platform_stack` is ever created, so its teardown never fires against live shared containers. | Warn | [PLAT-TASK-005, CI] | Owner: Engineer — the `stack` marker exclusion is a correct but easy-to-forget convention; document it in `testing-strategy.md` or CI job config (always pair `docker` marker runs with `and not stack` unless deliberately isolating `test_local_stack.py` alone) before PLAT-TASK-005's CI job is wired up. |
| PLAT-EPIC-004 PR review | Opportunistic cleanup, not fixed now: (a) `require_workspace_role`/settings/sparql do workspace-exists-then-role as two sequential DB round trips — collapsible to one LEFT JOIN of workspaces↔workspace_members; (b) `ensure_human_principal`/`ensure_agent_principal` in identity/registry.py share upsert boilerplate → one `_upsert_principal`. | Info | [PLAT-TASK-005, v1] | Consolidate when these files are next touched — not worth standalone churn. |
| PLAT-EPIC-003 PR review | Audit emission is a manual per-route call and was already forgotten once (settings write, caught by QA). Deeper mechanism (Postgres AFTER trigger, or route decorator) would make omission impossible. S3 tenant prefixing likewise relies on caller discipline (`storage/tenant_objects.py`). | Warn | [PLAT-TASK-009] | Owner: Engineer — when TASK-009 builds the hash-chained store, move emission to a structural mechanism and narrow the S3 API to `put_tenant_object(tenant_id, ...)`. |
| PLAT-TASK-003 | RESOLVED — `PUT /api/settings/{key}` (`routers/settings.py::set_setting_route`) never called `default_audit_emitter.emit(...)`, the only one of the four mutation routes missing it. Fixed by Engineer in 615b764 (red test `test_settings_write_emits_audit_event`) + aabb90b (emit call added, mirrors the other 3 call sites). QA re-verified: diff placement correct (inside `tenant_connection`, success path only), fast suite 70 passed/22 deselected, docker suite 9 passed (incl. new test against live stack), ruff+mypy clean. No longer blocks TASK-009. | Resolved | [PLAT-TASK-009] | Owner: Engineer — none, fix accepted. TASK-009 may now trust this seam's completeness for the settings route. |
| PLAT-TASK-005 QA | Ledger item 3 (search membership-authz) CLOSED for `GET /api/search`: independently re-ran `test_search_rejects_non_member_of_workspace` (zero-membership → 403) and `test_search_rejects_foreign_tenant_workspace_id` (foreign tenant's real `workspace_id` → 404 before 403, IDOR-safe) against a live docker stack — both pass, ADR-008 documents the resolution order honestly. | Resolved | [PLAT-TASK-005] | Owner: none — fix accepted. |
| PLAT-TASK-005 QA | Ledger item 2 (visual baselines) partially closed: gating is real (`tests/e2e/visual-baselines.spec.ts` registers 0 tests on this macOS box, confirmed by direct run; no `.png` baseline committed, confirmed by `git status`/diff). Still ACCEPTED-PENDING, not fully resolved: CI's first Linux run must execute `playwright test --update-snapshots` to seed `tests/e2e/__screenshots__/` before the visual half of Category 15/17 is real for this task's screens — no evidence that step has happened yet. | Accepted-pending | [PLAT-TASK-005, CI] | Owner: Engineer/CI — seed baselines on first Linux CI run of this branch; re-verify at phase gate. |
| PLAT-TASK-005 QA | Ledger item 1 (design-token backfill, PLAT-TASK-002 row above) partially closed with a NEW fallout defect — see FAIL-3 row below: the backfilled `--text-caption` token was paired with `--color-text-subtle` in `dashboard-placeholder.tsx`'s footer, directly contradicting `typography.md`'s own written rule ("`--text-caption` must use `--color-text-default` or `--color-text-muted`, never `subtle`") and failing WCAG 1.4.3 (measured contrast ratio ~3.2:1 on the dark theme, ~4.5:1 minimum required for text this small). | Fail | [PLAT-TASK-005] | Owner: Engineer — see FAIL-3. |
| PLAT-TASK-005 QA | FAIL-1: `tests/e2e/auth.spec.ts:18` (written in PLAT-TASK-002, commit 2a75ce3) asserts the principal IRI renders as `"urn:weave:principal:dev-user-1"` (no `user:` segment). `identity/registry.py`'s canonical IRI format (commit 439828e, PLAT-TASK-004, predates PLAT-TASK-002's own test chronologically in the merge graph even though the task numbers suggest otherwise — verified via `git merge-base --is-ancestor`) emits `"urn:weave:principal:user:dev-user-1"`. This is a genuine, 100%-reproducible failure (3/3 identical runs, fresh dev server each time, not a timing flake) that predates PLAT-TASK-005's own commits but blocks it: `ui_verify.sh --full` step B runs the app's *entire* Playwright suite, so this stale assertion fails the Category 17 hard gate for PLAT-TASK-005 (and will fail it for every UI task after, until fixed). | Fail | [PLAT-TASK-002, PLAT-TASK-004, PLAT-TASK-005, all-future-UI-tasks] | Owner: Engineer — update `auth.spec.ts:18`'s expected string to match the canonical `user:`-prefixed IRI format (the format is correct/intentional per PLAT-TASK-004's ADR; the test is stale). Small fix, should land before the next `ui_verify` run on any branch. |
| PLAT-TASK-005 QA | FAIL-2: the documented "run `npx playwright test` as ONE invocation" mitigation for the shared in-memory auth rate-limiter (5 req/60s, `lib/rate-limit.ts`) only serializes tests *within* `global-search.spec.ts` (`test.describe.configure({mode: "serial"})`). It does not serialize *across* spec files. Under Playwright's local defaults (`fullyParallel: true`, `workers: 4` since `process.env.CI` is unset locally), `auth.spec.ts` and `global-search.spec.ts` run concurrently, and their combined login attempts trip the shared limiter — reproduced 3 times independently (once: Cmd+K combobox never renders, 5s timeout; twice: page stuck at the mock-OIDC `/authorize` URL, never reaching `/dashboard`). The suite passes reliably only with `--workers=1` (which is what CI happens to force, but is not what a human or QA runs by typing the literal DoD command locally). `ui_verify.sh` itself invokes `npx playwright test` with no `--workers` override, so **this is the actual root cause of the Category 17 `ui_verify.sh --full` FAIL** for this task (alongside FAIL-1). | Fail | [PLAT-TASK-005, ui_verify-consumers] | Owner: Engineer — either force `workers: 1` for local (non-CI) runs too in `playwright.config.ts` (safe, small diff — the suite is fast enough serial that parallelism buys little locally), or move the rate-limiter test-mode bypass one level up (a `x-forwarded-for` test header per worker) so concurrent logins from different spec files don't collide. Recommend the former: `workers: process.env.CI ? 1 : 1` is a one-line change, ponytail-simplest fix. |
| PLAT-TASK-005 QA | FAIL-3: `components/dashboard/dashboard-placeholder.tsx`'s M2-availability footer (AC-6) renders `text-[length:var(--text-caption)] text-[var(--color-text-subtle)]` — `--color-text-subtle` (`#5c6779` dark / `#64748b` light) against `--color-surface` (`#0e1420` dark) measures ≈3.22:1 contrast, below the 4.5:1 WCAG AA floor for 11px text, and directly contradicts `docs/standards/design/typography.md`'s own stated rule that `--text-caption` "must use `--color-text-default` or `--color-text-muted`, never `subtle`". Confirmed three independent ways: (1) hand-computed relative-luminance contrast ratio, (2) real Chrome Lighthouse `color-contrast` audit (both mobile-simulated and `--preset=desktop`, against a production `next build`) flags this exact node, (3) new red test `tests/e2e/accessibility.spec.ts` (`@axe-core/playwright`, marked `test.fail()`) reproduces it live. Also the direct cause of the Lighthouse accessibility category scoring 0.95, not 100 (Category 15 hard bar). | Fail | [PLAT-TASK-005] | Owner: Engineer — swap `--color-text-subtle` for `--color-text-muted` (or `-default`) on that footer; one class-name change, no new token needed. |
| PLAT-TASK-005 QA | FAIL-4: `NAV_ITEMS` (AC-1) links to six routes with no page yet (`/ce`, `/build`, `/events`, `/explorer`, `/onboarding`, `/settings` — deliberate M1 scope per the file's own comment). Next.js's default `<Link>` prefetching issues real RSC-prefetch network requests for these on every page load, which 404 and are logged as browser console errors — this alone drops Lighthouse's `best-practices` category to 0.92-0.96 (not 100), the other cause (with FAIL-3) of the Category 15 Lighthouse-100 bar failing. | Fail | [PLAT-TASK-005] | Owner: Engineer — add `prefetch={false}` to `<Link>` in `nav.tsx` for any `NAV_ITEMS` entry with no built route yet (one-line fix per problem link, or a single conditional if the "route exists" list is centralized). |
| PLAT-TASK-005 QA | Gap (not a task FAIL, a coverage gap): `@axe-core/playwright` has been an installed dependency since scaffold but zero E2E specs used it before this session — the only a11y coverage was jsdom-based `vitest-axe` (`shell.a11y.test.tsx`), which cannot compute real paint/contrast and is exactly why FAIL-3 was invisible to unit tests. QA added `tests/e2e/accessibility.spec.ts` this session (marked `test.fail()` pending the FAIL-3 fix) as the first real-browser axe pass; future UI tasks should extend this file per new screen rather than re-inventing jsdom-only checks. | Warn | [PLAT-TASK-005, future-UI-tasks] | Owner: Engineer — once FAIL-3 is fixed, remove the `test.fail()` annotation so the test asserts a real green, and add further screens (nav, help panel, command palette open-state) to the same file as they ship. |
| PLAT-TASK-005 QA | Related-but-distinct dimension of the still-open PLAT-TASK-002 Lighthouse-methodology finding (row above): Lighthouse was measured here against both `npm run dev` (scores 0.85/1.0/0.92/0.91) and a production `npm run build && npm run start` (0.99-1.0/0.95/0.92/0.91) on `/dashboard`. Dev mode inherently cannot reach 100 (unminified JS, dev-only console warnings) — the harness's own `playwright.config.ts` webServer and this task's own summary both use `npm run dev`, meaning **every Lighthouse-gated QA pass so far may have been measuring the wrong artifact**. Not escalated to `qa-project-issues.md` yet (distinct recommendation from PLAT-TASK-002's preset-ambiguity finding, not a verbatim repeat — Law #11 requires an identical repeated recommendation), but flagged loudly: the Architect fix for PLAT-TASK-002's open item should address *both* preset AND prod-vs-dev-build in the same `lighthouserc.json`/`testing-strategy.md` note. | Warn | [PLAT-TASK-005, PLAT-TASK-003, PLAT-TASK-004, all-Lighthouse-gated-tasks] | Owner: Architect — combine with the open PLAT-TASK-002 item; if a third occurrence lands in the next QA report, escalate both together per Law #11. |
