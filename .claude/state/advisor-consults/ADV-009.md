# ADV-009 — Correct stale "no server-side branch protection" claims (protection now LIVE)

- **Date:** 2026-07-20
- **Advisor:** opus (`advisorModel`)
- **Scope:** planned harness change — GitHub branch protection on `main` went live (observed
  2026-07-19: direct push rejected by repository rules, 12 required status checks, auto-merge
  disabled; PR #191 merged manually after checks). Four harness files still claim the repo has NO
  server-side protection: `.claude/rules/git-safety.md` (force-push bullet, "SOLE enforcement"),
  `.claude/rules/harness-governance.md` ("Honest limit" closing sentence),
  `.claude/scripts/modules/git_safety.py` (module comment, which said "verified"),
  `.claude/scripts/tests/test_git_safety_force.py` (docstring). Comment/prose-only corrections;
  zero behavioural change to the hook. Human approval: explicitly requested by Gareth
  ("quick fix that then and push the PR").

## Verdict

**APPROVE-WITH-CHANGES**

## Blocking items (all addressed before commit)

1. The originally planned edit covered only the two rules files; the same stale claim also lives in
   `.claude/scripts/modules/git_safety.py:41-44` (worse — says "verified") and
   `.claude/scripts/tests/test_git_safety_force.py:8`. Fixed in the same change with mirrored
   wording. Historical documents describing the past state
   (`docs/design/review-findings-2026-07-19.md`, `docs/design/remediation-2-api-gaps.md`) left
   untouched by design.

## Advisory items (adopted)

1. State explicitly that force-push coverage by the remote ruleset is **unaudited** — the hook
   stays described as *primary* enforcement for force-push shapes, not a redundant layer.
2. harness-governance.md wording: "now adds a server-side backstop" instead of "now backs this
   locally" (server-side is the opposite of local).
3. No edit needed elsewhere: no other always-loaded rule or `CLAUDE.md` restates the claim;
   `docs/standards/code-review.md:79` is now consistent with the corrected files.

## Rationale (advisor's summary)

No gate weakened (rule 4 pass) — the change acknowledges *stronger* enforcement existing
server-side while keeping the local hook described as active/primary. Wording is
observed-not-audited ("observed 2026-07-19", concrete evidence listed) rather than claiming an
audited ruleset. Scope was correct for rules files but incomplete for the harness overall; the
module comment and test docstring are inside the enforcement core, so the same HITL approval and
this consult cover them.
