# ADV-006 — harness: stop.py docstring example + CLAUDE.md Law 5

- **Date:** 2026-07-08
- **Change under review:** uncommitted working-tree diff (author: Gareth, direct human edit)
- **Advisor model:** fable (`advisorModel`)
- **Verdict:** APPROVE

## Scope

Two harness files:

1. `.claude/scripts/modules/stop.py` (enforcement core) — docstring-only.
2. `CLAUDE.md` (root, always-loaded operating instructions).

## Findings

1. **`stop.py` is comment-only.** The single hunk changes one line inside the `drift_check`
   docstring: the `LLM_MODEL` example identifier goes from `"gemma4:e4b"` to
   `"batiai/qwen3.6-27b:iq3"`. No code, control flow, config parsing, or gate logic is touched.
   Confirmed via `git diff` — the change is inside the documentation block, not executable code. Zero
   enforcement impact.

2. **`CLAUDE.md` adds Law 5** — a communication/output rule requiring obscure identifiers
   (e.g. TASK-007, EPIC-823, FR-37) to be accompanied by plain-language explanation. This is an
   additive style constraint on agent output; it removes no existing law, weakens no gate, and does
   not conflict with any `.claude/rules/*.md` (it complements the existing "talk in plain language /
   ELI5" protocol). Laws 0–4 are unchanged.

No gate is weakened, no enforcement behaviour is altered, and no rule conflict is introduced. Both
edits are human-authored, so harness-governance rule 3 (sonnet self-modification) does not apply.

## Conditions

None. Note: per harness-governance rule 2, `stop.py` and `modules/**` are enforcement core and still
require the deterministic HITL approval prompt on the Edit/Write, independent of this advisor verdict.
Put `Advisor-Consult: ADV-006` on the commit.
