# ADV-002 — CI/security: make #12 green (build placeholders, mutation altitude, semgrep triage + blocking)

- **Date:** 2026-07-04
- **Change under review:** the CI/security batch that greened PR #12 and hardened the gates
  (commits `6f86a97`, `59510ca`, merged to main; plus follow-up on `harness/ci-hardening`).
- **Advisor model:** fable (`advisorModel`)
- **Verdict:** APPROVE-WITH-CHANGES

## Scope reviewed

- `.github/workflows/ci.yml`: build-time auth placeholders for `next build`; mutation job
  `continue-on-error` (relocate 70% gate to the phase gate); semgrep flipped to **blocking**.
- 6 inline `nosemgrep` waivers: 2× asyncpg-sqli (`tenancy/workspaces.py`), 4× infra
  (`cloudfront`/`dynamo_lock`/`secrets`/`vpc` `main.tf`).

## Advisor findings + dispositions

1. **soon — bare `# nosemgrep` too broad (workspaces.py:36,64).** Suppressed *every* rule on the
   DB-touching line, not just asyncpg-sqli. RESOLVED on `harness/ci-hardening`: rule-scoped to
   `python.lang.security.audit.sqli.asyncpg-sqli.asyncpg-sqli` on a standalone comment line directly
   above the call (fits the 100-char lint; semgrep honours line-above; verified exit 0). The waivers
   themselves confirmed honest — static literal SQL, all values bound as `$1..$5`.
2. **soon — semgrep job unpinned while now blocking.** RESOLVED: image pinned to
   `semgrep/semgrep:1.168.0@sha256:59fbed…` (matches the file's SHA-pinning convention). Residual
   accepted risk — `--config auto` still pulls the live registry ruleset, so an upstream rule
   rename can redden a PR or invalidate a waiver → logged as **PROJ-004**.
3. **nit — mutation score near-invisible.** RESOLVED: gate step now tees the score to
   `$GITHUB_STEP_SUMMARY` (pipefail preserves the honest non-zero exit; job stays non-blocking).
   The 62.1%→70% gap logged as **PROJ-005**. Advisor confirmed the ≥70% gate genuinely exists at the
   phase gate (`implement`/`phase-gate` SKILL) — so this is git-safety rule 4 "better altitude", not
   removal.

## Verified-clean (no action)

- Build placeholders can't reach the client bundle (not `NEXT_PUBLIC_*`) and can't mask a prod
  missing-secret (`requireAuthSecret`/client-secret re-throw at server runtime when
  `NODE_ENV=production`; the CI build artifact is never deployed — deploy job is terraform-only,
  main-only).
- All 4 infra waivers legitimate (default cert is dev-only; SSE-enabled lock table; AWS-managed KMS
  default for secrets; public-IP only on the public subnet).
- Semgrep flip to blocking = strict gate strengthening.

## Note

ci.yml, infra/terraform, and backend source are **governance-exempt** paths, so no consult was
*required* — this consult was run at the human operator's explicit request before starting the
implement loop, and is recorded here for the trail.
