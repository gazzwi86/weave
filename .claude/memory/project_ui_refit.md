---
name: UI refit program (piece-by-piece, mock-first)
description: Rebuilding the built app's UI section by section against a living mock
  (docs/design/mocks/refit-mock.html); phase 1 (app shell) signed off 2026-07-17.
type: project
created: 2026-07-17
expires: 2026-09-01
---

The built app diverged from the approved V4/V5 design direction. Recovery process (user-agreed
2026-07-17): for each section, (1) extend the living mock `docs/design/mocks/refit-mock.html`,
(2) user signs off, (3) apply to the app via the Storybook atomic library, (4) screenshot/browser
verify, (5) user signs off, next section. MOCK COMPLETE + FULLY SIGNED OFF 2026-07-17 (all sections: shell, Constitution incl. canvas
overlays/authoring/publish, Audit, Build, Settings, Home, operator console, explainer tooltips,
error patterns — toasts + inline error cards, New-entity menu). ROLLOUT to the real app underway:
Storybook components first, then app containers, screen by screen with council reviews, visual
passes, and UI/visual-regression tests written as we go. Method: sonnet/opus engineers get very
detailed specs for the first pass; Fable steps in only where they fail. UX pattern law from the
mock: drawers for edit/read, centred modals only for confirm/pickers/add-person; toasts for
feedback; inline error cards with retry for failed loads; every destructive action confirms.
API/system gaps discovered during rollout are appended to docs/design/remediation-2-api-gaps.md.
Rollout working decisions (2026-07-18, Fable recommendations adopted under /goal autonomy; user may
override): async section sign-offs (build doesn't block on review); Explore canvas = restyled
cytoscape (kind shapes/colours via node styles), fcose layout w/ wide spacing + deterministic seed;
landing page = Home; v1 QA/baselines dark-only (light functional, unverified); operator console =
role-gated UI now, backend endpoints as tail gap lane; pushing/PRs authorized (user MCQ 1a,
2026-07-18). STANDING RULE: during the rollout every turn ends with a running lane or an explicit blocker.
`mock-v5-delta.html` stays the visual north star; refit-mock is the working artefact.
BURN-DOWN STATUS 2026-07-19: the full-app review findings are ~2/3 closed (18 PRs, #171–#188 —
all H-severity done except C5/explore-labels); demo seed enriched (#184). Remaining work is
consolidated in ISSUES.md (Design squad = the design-shaped rebuilds: notifications page,
billing, operator console, profile, new-instance authoring, C5+V3b explore); tracker checkboxes
in remediation-2-api-gaps.md stay canonical for done/open.

**Why:** User reviewed the autonomous build and found nav, styling, animations, and several pages
broken or off-design; trusts high-tier model for UI direction with right-sized agents executing.

**How to apply:** Before UI work, check the current phase here; don't jump ahead of sign-off.
A `BLOAT_REPORT.md` (repo root) catalogues duplication/DRY/YAGNI findings for later action —
generated by scout fleet 2026-07-17. Logo: use the node-graph W SVG mark
(`docs/design/logo-mark.svg`, also `packages/frontend/public/`), not the padded `logo.png`.
See [[feedback_dumb_smart_components]] and [[feedback_no_phase_pills]].
