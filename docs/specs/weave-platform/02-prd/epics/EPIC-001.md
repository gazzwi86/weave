# Epic: EPIC-001 - Generative Dashboard (composition + lifecycle)

## Overview

**Phase:** Phase 2 (MVP) — Generative Dashboard & CE-Sourced Widgets (needs Constitution Engine GA)
**PRD Reference:** [prd.md](../prd.md#epic-1-generative-dashboard-composition--lifecycle)
**Status:** Backlog
**Priority:** Must Have

## Description

The user-facing centrepiece: a declarative generative-UI surface where a workspace member describes
what they want and the AI composes the best-fit widget from a finite component library and streams it
into the dashboard grid. This epic owns the full widget lifecycle — generate, choose component type,
refine, pin (server-side per-user), publish to the workspace library, and role-appropriate starters —
all bound to live provider metrics contracts, never to free-form code.

## User Stories

| Task ID | Title | Status | Priority |
|---------|-------|--------|----------|
| TASK-001 | Request a widget by describing what you want (E1-S1) | Backlog | Must Have |
| TASK-002 | AI picks the best component type for the intent (E1-S2) | Backlog | Must Have |
| TASK-003 | Refine a widget after generation (E1-S3) | Backlog | Must Have |
| TASK-004 | Pin a widget (server-side, per-user) (E1-S4) | Backlog | Must Have |
| TASK-005 | Publish a widget to the workspace library (server-side, team-shared) (E1-S5) | Backlog | Must Have |
| TASK-006 | Default starter widgets (first load) (E1-S6) | Backlog | Must Have |
| TASK-007 | Prompt examples and suggestions (E1-S7) | Backlog | Should Have |

## Acceptance Criteria (Epic Level)

- [ ] Intent → component mapping is purely declarative: across all stories, no generated widget ever
      results in free-form code or a chart type outside the finite library (KPI, line/area, bar, list,
      activity feed, pie/donut, heatmap, banner, table) — a single intent-mapping audit confirms every
      path resolves to exactly one library component or declines.
- [ ] Every failure mode resolves to a defined, named state, never a blank or hallucinated widget:
      provider-unavailable, LLM-503, budget-cap-mid-stream, unsatisfiable-prompt, and refresh-error
      each render their specified state — verified by one end-to-end failure-mode sweep, not per story.
- [ ] Widget persistence boundaries hold across the lifecycle: a pinned widget is (tenant, user)-scoped
      and invisible to another user/tenant, while a published widget is workspace-scoped and addable as
      an independent per-user copy — a single cross-user/cross-tenant test proves pin and publish do not
      leak across these boundaries.
- [ ] Only MVP-eligible (CE-sourced) categories are offered as starters or example prompts; no starter
      or suggestion references a non-GA source engine — verified by inspecting the starter and example
      sets against the live-provider list.
- [ ] Every rendered widget (generated, refined, pinned, published, starter) carries a data-source
      footer label naming its contract(s).

## Dependencies

- **Blocked by:** Phase 1 platform shell (EPIC-003 tenancy/settings cascade for budget caps and pin
      scoping, EPIC-004 auth/RBAC for author-permission and audit-visibility, EPIC-008 `PLAT-BILLING-1`
      for the mid-stream budget-cap halt). Constitution Engine GA — consumes `CE-METRICS-1`, `CE-READ-1`,
      `CE-DIFF-1`, `CE-VERSION-1`, `CE-EVENT-1`.
- **Blocks:** EPIC-002 (Widget Library) — the library categories render inside the composition/lifecycle
      machinery this epic delivers.

## Technical Notes

- Declarative generative-UI pattern only: intent maps to a fixed component set. The mapping rule
      (count/status→KPI, trend→line/area, comparison→bar, ranked→list, log→activity feed, ratio→pie/donut,
      two-dim→heatmap, alert→banner) and the named-type override live in the tech spec.
- Streaming targets are configurable defaults (streaming header/skeleton ≤ 1 s; refinement history 10
      steps; auto-refresh 5 min; grid 1–4 columns; example-prompt count 4–6, dismiss after 3 widgets) —
      all tunable per workspace, owner Architect for provisional values.
- Pin and publish are **server-side** state (never localStorage): pins scoped to (tenant, user),
      library entries workspace-scoped, both RBAC-scoped and audit-visible via `PLAT-AUDIT-1`.
- Budget caps resolve through `PLAT-SETTINGS-1`; LLM 503 must surface the prototype `LlmBar` 503
      behaviour as a readable, retryable offline state.

---
*Generated by Weave Architect agent.*
