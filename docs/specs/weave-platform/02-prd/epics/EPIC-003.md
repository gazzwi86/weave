# Epic: EPIC-003 - Tenancy, Workspaces & Settings Cascade

## Overview

**Phase:** Phase 1 (MVP) — Platform Shell & Cross-Cutting Foundations
**PRD Reference:** [prd.md](../prd.md#epic-3-tenancy-workspaces--settings-cascade)
**Status:** Backlog
**Priority:** Must Have

## Description

The multi-tenant spine: create and switch tenant contexts across the 4-level cascade
(Company → Domain → Workspace → Project), invite and manage members with bounded revocation, and resolve
settings down the cascade with tighter-wins inheritance. This epic owns `PLAT-SETTINGS-1`, the single
contract through which budgets, retention, data classification, and RBAC are resolved for every engine.

## User Stories

| Task ID | Title | Status | Priority |
|---------|-------|--------|----------|
| TASK-001 | Create and switch workspaces (4-level cascade) (E3-S1) | Backlog | Must Have |
| TASK-002 | Invite and manage members (bounded revocation) (E3-S2) | Backlog | Must Have |
| TASK-003 | Settings cascade — budgets, retention, policy, RBAC (E3-S3) | Backlog | Must Have |

## Acceptance Criteria (Epic Level)

- [ ] Tenant isolation holds at the storage layer across every backing store: a query in tenant A's
      context returns zero rows from tenant B's seeded data across RDF, Aurora, and S3 Vectors, and an
      unscoped SPARQL query is rejected — verified by the cross-tenant-read isolation test, independent
      of any single story passing.
- [ ] The settings cascade is monotonic in one direction: at any node the resolution API returns both
      the effective value and the level that set it, a child may only tighten a parent value, and any
      loosening attempt without parent approval is rejected and recorded to `PLAT-AUDIT-1` — proven for
      all four governed setting classes (budget, retention, classification, RBAC) in one cascade test.
- [ ] Member revocation and context-switch authorization share one enforcement mechanism: the next
      request bearing a removed/changed member's prior token is rejected within the bounded revocation
      latency (default ≤ 60 s), and an unauthorized context switch loads zero cross-tenant data with a
      403 — the same short-token-TTL + session-version check backs both, consistent with FR-021/FR-024.

## Dependencies

- **Blocked by:** Nothing upstream — Phase 1 has no engine dependency and ships first. Relies on the
      Phase-1 multi-tenant isolation mechanism decision (OQ-01) and the dev-AWS Cognito pool.
- **Blocks:** Every other platform epic and every engine. `PLAT-SETTINGS-1` resolves RBAC for EPIC-004,
      budget caps for EPIC-008, retention for EPIC-009, and tenant scoping for EPIC-001/EPIC-002 pin and
      library persistence.

## Technical Notes

- `PLAT-SETTINGS-1` is the single owner of the 4-level tighter-wins cascade; no engine re-implements
      settings resolution. The resolution API must return effective value plus originating level.
- Revocation uses short-lived access tokens (default TTL ≤ 60 s, tunable) plus a per-request
      session-version check against a revocation list — one mechanism, one bounded latency; "immediate"
      is replaced by the bounded value. This is the same mechanism EPIC-004's RBAC enforcement consumes.
- Workspace creation requires name, slug, parent (Domain), and billing plan; the new node inherits
      parent settings under tighter-wins.
- Data-retention config is per data type (audit, PROV-O, model versions) within platform minimums and
      resolves through the cascade.

---
*Generated by Weave Architect agent.*
