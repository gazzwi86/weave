# Epic: EPIC-007 - Decision Log (view over the platform audit service)

## Overview

**Phase:** Phase 1 (MVP — S1 view) · Phase 2 (S2 export)
**PRD Reference:** [prd.md](../prd.md#epic-7-decision-log-view-over-the-platform-audit-service)
**Status:** Backlog
**Priority:** Must Have (S1) / Should Have (S2)

## Description

The Decision Log records every agent action and HITL approval immutably so a compliance officer can
audit what the dark factory did and why. Critically, Build keeps **no** independent signed store —
both the Decision Log screen and the per-task Audit tab are filtered views over `PLAT-AUDIT-1`.
Export (Phase 2) is delegated to the platform's query+export interface with signature metadata.

## User Stories

| Task ID | Title | Status | Priority |
|---------|-------|--------|----------|
| TASK-001 | Per-project decision log as a view over PLAT-AUDIT-1 (E7-S1) | Backlog | Must Have |
| TASK-002 | Decision-log export (E7-S2) | Backlog | Should Have |

## Acceptance Criteria (Epic Level)

- [ ] Build holds no independent audit store: the Decision Log screen, the per-task Audit tab
      (E5-S2), and export (S2) are all filtered views/delegations over `PLAT-AUDIT-1` — there is no
      Build-side signed log that could diverge from the platform's hash-chained record.
- [ ] The system is fail-closed on audit: when `PLAT-AUDIT-1` is unreachable, any mutating agent
      action is **refused** rather than performed un-audited, and BLOCKED operations (sandbox
      violations) are recorded and surfaced but never deletable.
- [ ] Every recorded event carries the canonical actor principal IRI from `PLAT-IDENTITY-1`, so a
      HITL approval's approving identity and an agent's BLOCKED op are attributable to a real
      principal across both the screen and the export.
- [ ] Export (S2) preserves signature-verification metadata consistent with the platform chaining
      scheme and paginates rather than silently truncating when it exceeds the platform size/time
      budget.

## Dependencies

- **Blocked by:** Weave Platform `PLAT-AUDIT-1` (the immutable store, query + export) and
  `PLAT-IDENTITY-1` (principal IRIs).
- **Blocks:** EPIC-006 (BLOCKED ops + approver identity must be logged for the no-self-approval
  invariant); EPIC-005 (Task Detail Audit tab); EPIC-012 (phase-gate ceremony references the
  decision record).

## Technical Notes

- Append-only, tamper-evidence (hash-chain + signing) is enforced by `PLAT-AUDIT-1` at the
  DB-constraint level — not reimplemented in Build; delete attempts are rejected and themselves
  logged.
- The fail-closed-on-mutation rule (refuse the action if audit is unreachable) is a cross-cutting
  invariant binding every mutating agent step across the engine, not just the Decision Log screen.
- Export is JSON/NDJSON, filtered by date/agent/op, delegated to the `PLAT-AUDIT-1` query+export
  interface (FR-028); it is Phase 2 per the roadmap.

---
*Generated by Weave Architect agent.*
