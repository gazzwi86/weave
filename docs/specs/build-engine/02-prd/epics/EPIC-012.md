# Epic: EPIC-012 - Quality Gates & Spec-Coverage

## Overview

**Phase:** Phase 1 (MVP — S1–S6) · Phase 2 (S7 finding propagation, S8 reality-drift)
**PRD Reference:** [prd.md](../prd.md#epic-12-quality-gates--spec-coverage)
**Status:** Backlog
**Priority:** Must Have (S1–S6) / Should Have (S7–S8)

## Description

Epic 12 owns the task- and phase-level quality ceremonies that wrap generation: the
Definition-of-Ready and Definition-of-Done gates, the full QA category suite, the cumulative
spec-coverage audit, the phase-gate ceremony, the pre-scaffold spec-review gate, cross-task finding
propagation, and reality-drift detection. It references E8-S1's generation gates as a subset rather
than restating them, ensuring "done" and "ready" are measured, never asserted.

## User Stories

| Task ID | Title | Status | Priority |
|---------|-------|--------|----------|
| TASK-001 | First-class Definition-of-Ready gate (E12-S1) | Backlog | Must Have |
| TASK-002 | First-class Definition-of-Done gate (E12-S2) | Backlog | Must Have |
| TASK-003 | Full QA category suite (E12-S3) | Backlog | Must Have |
| TASK-004 | Cumulative spec-coverage audit at phase end (E12-S4) | Backlog | Must Have |
| TASK-005 | Phase-gate ceremony with security, mutation, and doc-generation (E12-S5) | Backlog | Must Have |
| TASK-006 | Pre-scaffold whole-spec review gate (E12-S6) | Backlog | Must Have |
| TASK-007 | Cross-task finding propagation and project escalation queue (E12-S7) | Backlog | Should Have |
| TASK-008 | Reality-drift detection (code vs spec) (E12-S8) | Backlog | Should Have |

## Acceptance Criteria (Epic Level)

- [ ] Gates are mechanical and self-run, never self-reported: the DoR checklist (any single FAIL →
      NOT READY) is verified in PLAN before DELEGATE, the DoD checklist names a tool/metric per item
      and the QA agent runs each command itself, and a QA pass that touched implementation code is
      invalid (E6-S2 boundary) — at no point does an agent vouch for its own readiness or doneness.
- [ ] Every "cannot evaluate" defaults to fail/halt, never to a silent pass: an unevaluable brief is
      NOT READY, an unrunnable DoD command is NOT VERIFIED (fail), unavailable QA-category tooling is
      NOT VERIFIED (fail), an unmappable `Must` item in the coverage audit is MISSING, and a
      ceremony-step error keeps the phase gate closed (fail-closed).
- [ ] A phase cannot close with silently-missing requirements: the cumulative spec-coverage audit
      halts the phase unless ≥90% of `Must` items are DELIVERED (default, tunable) **and** zero are
      MISSING, and the phase-gate ceremony then requires a human Approve/Amend/Reject subject to
      no-self-approval (E6-S4) — a CRITICAL security finding blocks Approve.
- [ ] Defects and contradictions are carried forward, not silently repeated: a defect affecting other
      tasks propagates with an `affects:[task_id…]` list that later QA passes MUST read, a
      recommendation recurring ≥2× is promoted to a project issue, and reality-drift contradictions
      are surfaced for human decision (never auto-resolved).

## Dependencies

- **Blocked by:** EPIC-005 (the brief's DoR/DoD checklists + AC-to-test map the gates verify);
  EPIC-008 (E8-S1 generation gates the DoD subsumes); EPIC-011 (`phase-complete` query the ceremony
  evaluates; PLAN stage the DoR runs in); EPIC-007 (`PLAT-AUDIT-1` for the no-self-approval record);
  Weave Platform `PLAT-NOTIFY-1` (web HITL Approve/Amend/Reject), `PLAT-SETTINGS-1` (tunable gate
  thresholds).
- **Blocks:** Scaffolding (E12-S6 pre-scaffold spec-review must pass before any build is scaffolded);
  phase advancement for every phase (the phase-gate ceremony is the gate); EPIC-006 (findings surface
  in Blockers & Escalations).

## Technical Notes

- **Partition with Epic 6 / Epic 8:** E8-S1 owns the generation gates that run before a single
  commit; Epic 12 owns the task/phase ceremonies that wrap them and reference E8-S1's gates as a
  subset of the DoD.
- DoD defaults (tunable via `PLAT-SETTINGS-1`): lint 0 errors, type-check clean, complexity
  cyclomatic ≤10 / cognitive ≤15 / function length ≤50, coverage ≥80%, mutation ≥70%, SAST 0-high,
  boundary validation, no `eval`/`Function()`, docs updated, conventional-commit hygiene.
- The QA category suite (E12-S3) includes a **browser-automation-with-backend-assertion** check (a UI
  action is verified to have produced the expected backend effect, not just a screenshot) and a11y
  (axe / WCAG 2.1 AA, zero violations on UI surfaces).
- The former E12 self-improvement proposal taxonomy (PROMPT/WORKFLOW/RULE/CONTEXT/MODEL) is folded
  out — owned by Weave Platform via `BE-SELFIMPROVE-1` (A3); Build surfaces proposals read-only
  (FR-012).
- S7 (finding propagation) and S8 (reality-drift) are Phase 2; S1–S6 are the MVP slice.

---
*Generated by Weave Architect agent.*
