# Epic: EPIC-001 - Ontology Modelling (OWL Structure)

## Overview

**Phase:** Phase 1 — Validated-Mutation Spine (MVP)
**PRD Reference:** [prd.md](../prd.md#epic-1-ontology-modelling-owl-structure)
**Status:** Backlog
**Priority:** Must Have

## Description

Lets an enterprise architect build the client domain taxonomy on top of the shipped ArchiMate-3
process-centric **BPMO framework** — defining OWL classes, property restrictions, and disjointness
axioms — through natural-language chat or guided forms, never raw RDF. Provides the structural
backbone that every other content area (instances, glossary, governance) attaches to. OWL reasoning
over the authored structure (E1-S4) is carved out to the reasoning phase and is not delivered here.

## User Stories

| Task ID | Title | Status | Priority |
|---------|-------|--------|----------|
| TASK-001 | E1-S1 — Define a new class via chat or form | Backlog | Must Have |
| TASK-002 | E1-S2 — Define property restrictions and disjointness | Backlog | Must Have |
| TASK-003 | E1-S3 — Import and refine a starting model | Backlog | Must Have |
| TASK-004 | E1-S4 — OWL reasoning surfaces inferences | Backlog | Should Have |

> E1-S4 (OWL reasoning surfaces inferences) is **carried to the publish-time reasoning phase** — it
> depends on the OQ-01 reasoner decision and the reasoning delivered in EPIC-008; it is not built in
> the Phase-1 slice of this epic.

## Acceptance Criteria (Epic Level)

- [ ] The Ontology / Types screen reflects the **active** ontology — the shipped framework kinds
      plus the client-built taxonomy — with no hardcoded client class IRIs anywhere (FR-020); the
      same screen renders correctly whether a class was authored via chat, form, or import.
- [ ] Every structural change authored across all three stories commits through the **single**
      validated mutation entry point (CE-WRITE-1) and produces a PROV-O record; no story introduces
      a write path that bypasses clone-then-validate.
- [ ] With the AI provider unavailable, an architect can still author a class, add a restriction,
      and add a disjointness axiom end-to-end via guided forms; every NL surface across the three
      stories returns HTTP 503 with an offline message while forms stay live.
- [ ] A `sh:Violation` at commit blocks with HTTP 422 and leaves the graph unchanged; OWL logical
      inconsistency does NOT block at commit here (it blocks at publish — EPIC-008/E8-S2), and this
      boundary holds for classes, restrictions, and imported proposals alike.

## Dependencies

- **Blocked by:** EPIC-006 (SHACL validation pipeline — the single mutation entry point CE-WRITE-1
      that every class/restriction commit must route through); EPIC-009 (PROV-O stamping); EPIC-011
      (chat + forms authoring surfaces). Upstream: PLAT-IDENTITY-1 (principal IRIs), PLAT-AUDIT-1.
- **Blocks:** EPIC-002 (instances attach to client classes defined here); EPIC-003 (glossary terms
      pun with OWL classes); EPIC-008 (reasoning operates over the authored OWL structure).

## Technical Notes

- Operates on the **shipped BPMO framework** (decision A1): the **13 process-centric kinds**
  (Process, Activity, Event, DataAsset/Field, System, Service, BusinessCapability, BusinessDomain,
  Policy, Goal, Actor, Concept, Class — Concept+Class are one punned resource per B1) + the
  relationship set + W3C SHACL/PROV-O/SKOS scaffolding. ArchiMate-3-aligned, with REA + UFO behind
  the curtain. Authoring extends it; it never ships a populated taxonomy.
- OWL/SHACL split (the "Polikoff rule"): OWL DL carries class semantics (open-world); SHACL carries
  data-quality enforcement (closed-world). Restrictions/disjointness are OWL axioms; the relationship
  matrix lives in SHACL shapes, not OWL.
- Class creation is **punning-aware**: a class URI may simultaneously be a `skos:Concept` (decision
  B1, surfaced by EPIC-003). Do not introduce a separate linking property.
- Import (E1-S3) commits **per accepted proposal** so history stays granular; reject-individual and
  ask-alternative must work without restarting the import session (FR-033).
- Cross-cutting standards: OWL 2 DL, Turtle serialisation, SHACL validation, PROV-O provenance.

---
*Generated by Weave Architect agent.*
