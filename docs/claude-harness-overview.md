---
type: Harness Documentation
title: Weave Harness Overview
description: "Complete guide to the Weave spec-driven dark-factory harness: directory layout, commands, SDLC flow, hooks, and the OKF knowledge graph."
tags: [harness, documentation, meta]
timestamp: 2026-06-29T00:00:00Z
resource: docs/claude-harness-overview.md
---

# Weave Harness — Complete Guide

> The Weave AI development harness. Read this before touching harness configuration.
> Last updated: 2026-06-29 (Pass 2 overhaul).

> **⚠️ Recent changes not yet folded into every section below (2026-06/07 harness-refine + patterns
> engagement — see `.claude/reports/H2`–`H5`). Where this guide and these disagree, these win:**
> - **UI-verification gate** — `.claude/scripts/ui_verify.sh` (functional click-through + structure/
>   links-up + axe + 8-state visual + Lighthouse; fail-closed) is re-executed by `phase-gate` and `qa`
>   (category 3l) and blocks on non-zero; the vision check is advisory; a human-signed run-book is required.
> - **Delivery = one PR per EPIC** (tasks stack on `feature/{epic}`, verified on the assembled epic), not per task.
> - **`--no-verify` is blocked** (`check-git-safety`); the pre-push manifest-parity gate (`check-harness-manifest`) is blocking.
> - **`.claude/HARNESS.md`** — derived element manifest, structural-parity pre-push gate.
> - **Per-entity, change-aware spec-review** (`/implement` Step 1.5), and **durable resume** from committed `progress.json` (Step 0).
> - **Golden patterns moved** to `docs/standards/patterns/` (was `.claude/spec-templates/few-shot/`), curated to this exact stack.
> - **Harness models pinned per tier** (`mid`/`high` — see `CLAUDE.md` §Stack) in `.claude/agents/*` + skills (the §12 table below predates this).
> - Operator runbook: **`docs/running-the-implement-loop.md`** is the current source of truth for the loop + HITL gates.

---

## 1. What the harness is

The Weave harness is a Claude Code configuration that turns Claude into a **spec-driven
dark-factory builder** for the Weave product itself. It implements:

1. **SDLC skills** — composable per-artifact skills that produce high-quality spec documents
   with research rounds, model right-sizing, and section-by-section HITL review
2. **Persona agents** — orchestration shells (PO, Architect, Engineer, QA) that call skills
   in sequence and hold the Laws, handoff contracts, and phase boundaries
3. **Dark factory loop** — an autonomous implementation mode that builds entire phases
   unattended, pausing only at phase-completion gates for human review
4. **Hooks** — lifecycle guards that enforce quality (no secrets, uv-only, anatomy freshness,
   phase-gate HITL, drift detection)
5. **OKF knowledge graph** — a unified Open Knowledge Format bundle spanning all docs; rendered
   to `docs/viz.html` for interactive browsing

**What it is NOT:** Weave's product features (Build Engine, Constitution Engine). This harness
builds Weave. The Build Engine is a separate sub-system that Weave will generate for customers.

---

## 2. Directory layout

```
docs/
├── specs/
│   └── weave/                      The single unified Weave spec
│       ├── weave-spec.md           Program plan (build order, MVP, gates, risks) + shared foundations
│       ├── contracts.md            Canonical inter-engine contracts (cite by ID)
│       ├── dev-environment.md      Local dev model, model routing, deploy boundary
│       └── engines/
│           ├── <entity>.md         ONE consolidated spec per engine — PO artifacts as sections:
│           │                         ## Brief · ## Product Requirements (PRD) ·
│           │                         ## Epics (### EPIC-NNN) · ## Roadmap
│           └── <entity>/           Architect artifacts (files — include non-markdown, so not sections)
│               ├── tech-spec/          (stack.md, architecture.md, openapi.yaml, data-model.md, …)
│               ├── decisions/ADR-NNN.md Architecture Decision Records (engine-lifetime)
│               └── m1/tasks/TASK-NNN.md Implementable task briefs, milestone-scoped
│                                        (sibling m2/, v1/, post-v1/ per roadmap milestone)
├── standards/                      Coding standards — OKF concepts
│   ├── code-style.md
│   ├── linting.md
│   ├── testing-py.md
│   └── ...
├── wiki/                           Code anatomy — OKF sub-bundle (generated by /anatomy)
│   ├── index.md                    OKF bundle root (okf_version only)
│   ├── log.md                      Change history (no frontmatter)
│   └── <area>.md                   One concept per source area
├── viz.html                        Unified OKF graph — gitignored; regenerate with /okf-visualize
├── index.md                        OKF bundle root for docs/ (okf_version only)
├── log.md                          OKF change log for docs/ (no frontmatter)
└── claude-harness-overview.md      This file

.claude/
├── skills/*/SKILL.md               Per-phase skill implementations
├── agents/                         Orchestration agent personas
├── commands/                       Top-level slash commands
├── spec-templates/                 Artifact templates (referenced by skills)
│   ├── brief.md
│   ├── prd.md
│   ├── roadmap.md
│   ├── epic.md
│   ├── adr.md
│   ├── phase-gate.md
│   ├── frontmatter-schema.md
│   ├── task.md
│   ├── tech-spec/                  Tech-spec section templates
│   └── standards/                  Per-language coding standard templates
│       ├── python/linting.md
│       ├── ts/linting.md
│       └── ...
├── state/
│   ├── progress.json               Task/phase state (always committed)
│   └── summaries/TASK-NNN.md       Per-task context chains
├── scripts/
│   ├── hooks.py                    Hook dispatcher
│   ├── modules/                    Hook modules (common, wiki, lifecycle, stop, ...)
│   ├── okf_validate.py             OKF conformance checker
│   ├── okf_visualize.py            OKF interactive graph renderer
│   ├── progress.sh                 Progress state CLI
│   └── statusline.sh               Status line for Claude Code UI
└── memory/MEMORY.md                Team knowledge — committed, shared
```

---

## 3. Command reference

### Primary workflow commands

| Command | What it does |
|---|---|
| `/po` | Product Owner orchestration: brief → PRD → roadmap → epics. Delegates to po-* skills, section-by-section HITL at each artifact. |
| `/architect` | Tech Architect orchestration: stack confirmation → C4 → OpenAPI → data model → flows → class → CI/CD → testing → DoD/DoR → infra → ADRs → task briefs. |
| `/elicit` | Structured elicitation using one of four methods: 20Q, Six Hats, Five Whys, Stochastic Reasoning. Use before `/po` for new problem areas. |
| `/implement` | Dark factory orchestration: PDAC loop (Plan → Delegate → Assess → Codify) per task, with phase-gate HITL. |
| `/qa` | 10-category task validation against the task brief. |
| `/spec-review` | Pre-scaffold spec completeness gate. Checks all artifacts before any code is written. |
| `/status` | Progress dashboard: kanban board, current phase, blocked tasks. |

### Knowledge graph commands

| Command | What it does |
|---|---|
| `/anatomy` | Full regeneration of `ANATOMY.md` + `docs/wiki/<area>.md` OKF bundle + `docs/viz.html`. |
| `/anatomy refresh <files>` | Incremental update: only the areas containing the named files are regenerated. |
| `/okf-validate` | Conformance check of the unified `docs/` OKF bundle. Hard errors (missing frontmatter, missing `type`) must be fixed before merge. |
| `/okf-visualize` | Render `docs/viz.html` — an interactive Cytoscape.js graph of all docs. |

---

## 4. The SDLC flow — step by step

### From idea to implementation

```
1. /elicit          Establish context — 20Q for new areas, Six Hats for contested ones
2. /po              Brief → PRD → roadmap → epics (section-by-section HITL at every section)
3. Human review     Read approved docs/specs/weave/engines/<entity>.md (all PO sections live there)
4. /architect       Stack → C4 → OpenAPI → data model → … → task briefs (HITL at every section)
5. /spec-review     Completeness gate — must pass before any code
6. /implement       Dark factory loop: auto-builds each task; phase gate at phase end
7. /qa              QA validates each task (auto-called by implement; run manually to recheck)
```

### The spec cascade

```
Elicitation
  └─ /elicit (20Q, Six Hats, Five Whys, Stochastic Reasoning)

Product Owner phase          [HITL: section-by-section within each artifact]
  └─ po-strategy  → docs/specs/weave/engines/<entity>.md (Brief → PRD → Roadmap, in strict sequence)
  └─ po-epic      → docs/specs/weave/engines/<entity>.md (one per epic)

Tech Architect phase         [HITL: section-by-section within each artifact]
  └─ arch-stack          → docs/specs/weave/engines/<entity>/tech-spec/stack.md
  └─ arch-diagrams       → docs/specs/weave/engines/<entity>/tech-spec/architecture.md,
                            class-diagram.md, business-process.md (C4 → class → flows)
  └─ arch-contracts      → docs/specs/weave/engines/<entity>/tech-spec/openapi.yaml, data-model.md
  └─ arch-quality        → docs/specs/weave/engines/<entity>/tech-spec/testing-strategy.md,
                            definition-of-done.md, definition-of-ready.md
  └─ arch-delivery       → docs/specs/weave/engines/<entity>/tech-spec/ci-cd.md, infrastructure.md
  └─ arch-adr            → docs/specs/weave/engines/<entity>/decisions/ADR-NNN.md (per key decision)
  └─ arch-task-brief     → docs/specs/weave/engines/<entity>/m1/tasks/TASK-NNN.md (batched 3-5 for HITL)

Implementation phase (dark factory)
  └─ /implement → phase-gated PDAC loop
      └─ Per task: PLAN → TDD → ASSESS → CODIFY (60-turn cap, Haiku-evaluated)
      └─ Phase gate: HITL Approve/Amend/Reject after each PO-defined phase
```

### Phase directories

| Phase | Artifact directory | Main artifact |
|---|---|---|
| Brief | `docs/specs/weave/engines/<entity>.md` → `## Brief` | brief section |
| PRD | `docs/specs/weave/engines/<entity>.md` → `## Product Requirements (PRD)` | PRD + `### EPIC-NNN` |
| Roadmap | `docs/specs/weave/engines/<entity>.md` → `## Roadmap` | roadmap section |
| Tech spec | `docs/specs/weave/engines/<entity>/tech-spec/` | architecture, openapi, data-model, flows, etc. |
| Tasks | `docs/specs/weave/engines/<entity>/m1/tasks/` (milestone-scoped; sibling `m2/`, `v1/`, `post-v1/`) | `TASK-NNN.md` |
| Decisions | `docs/specs/weave/engines/<entity>/decisions/` | `ADR-NNN.md` |

---

## 5. Skills reference

### Product Owner skills

| Skill | Invoked by | Produces | Model |
|---|---|---|---|
| `po-strategy` | `/po` → product-owner agent | `engines/<entity>.md` → Brief, PRD, Roadmap (strict sequence, each gated on prior approval) | High tier (elicit/stories) → mid tier (draft/NFRs) |
| `po-epic` | `/po` → product-owner agent | `engines/<entity>.md` → ### EPIC-NNN | Mid tier |

### Tech Architect skills

| Skill | Invoked by | Produces | Model |
|---|---|---|---|
| `arch-stack` | `/architect` → tech-architect agent | `<entity>/tech-spec/stack.md` | Mid tier + HITL |
| `arch-diagrams` | `/architect` → tech-architect agent | `<entity>/tech-spec/architecture.md`, `class-diagram.md`, `business-process.md` (C4 → class → flows) | High tier |
| `arch-contracts` | `/architect` → tech-architect agent | `<entity>/tech-spec/openapi.yaml`, `data-model.md` | Mid tier |
| `arch-quality` | `/architect` → tech-architect agent | `<entity>/tech-spec/testing-strategy.md`, `definition-of-done.md`, `definition-of-ready.md` | Mid tier |
| `arch-delivery` | `/architect` → tech-architect agent | `<entity>/tech-spec/ci-cd.md`, `infrastructure.md` | Mid tier |
| `arch-adr` | Architect, on decisions | `<entity>/decisions/ADR-NNN.md` | Mid tier |
| `arch-task-brief` | `/architect` → tech-architect agent | `<entity>/m1/tasks/TASK-NNN.md` | High tier |

### Orchestration and support skills

| Skill | Trigger | Purpose |
|---|---|---|
| `elicit` | `/elicit` | 20Q, Six Hats, Five Whys, Stochastic Reasoning |
| `implement` | `/implement` | Dark factory orchestration (PDAC loop) |
| `phase-gate` | Stop hook → implement | Phase completion HITL ceremony |
| `spec-review` | Pre-scaffold | Spec completeness gate |
| `status` | `/status` | Progress dashboard |
| `dependency-check` | Pre-phase | Tool and credential availability |
| `project-memory` | `/remember` | Save team facts to committed memory |
| `okf-validate` | `/okf-validate` | OKF conformance checker |
| `okf-visualize` | `/okf-visualize` | OKF interactive graph renderer |

---

## 6. Persona agents

Agents are orchestration shells. They hold Laws (non-negotiable constraints), a sequenced
list of skill invocations, and HITL contracts. They do NOT produce artifacts directly —
they delegate to skills.

| Agent | File | Triggered by | Calls skills |
|---|---|---|---|
| Product Owner | `agents/product-owner.md` | `/po` | po-strategy, po-epic |
| Tech Architect | `agents/tech-architect.md` | `/architect` | arch-stack, arch-diagrams, arch-contracts, arch-quality, arch-delivery, arch-adr, arch-task-brief |
| Engineer | `agents/engineer.md` | implement skill (subagent) | scaffold, TDD implementation |
| QA | `agents/quality-assurance.md` | implement skill (subagent) or `/qa` | validation + reporting (in-agent, no separate skill) |

### Agent Laws (universal, baked into every agent)

- **Law A** — common-stack first (Weave defaults, § Stack)
- **Law B** — functional, browser-runnable, automation-tested (Playwright for UI)
- **Law C** — council-graded quality for enterprise claims (5-persona, ≥ 4.0/5)
- **Law D** — stacked PRs (one PR per EPIC; small commits stack within a phase)
- **Law E** — complexity as a budget (cyclomatic ≤ 10, cognitive ≤ 15, fn ≤ 50 lines)
- **Law F** — synthetic verification only (LocalStack, not real cloud in tests)

---

## 7. Dark factory — how it works

The dark factory mode runs the implementation loop autonomously between human-review gates.

### Starting a run

```bash
/implement          # continue from current state (reads progress.json)
/implement TASK-003 # start a specific task
```

### PDAC loop mechanics

```
1. Read .claude/state/progress.json → next unstarted task whose deps are done
2. Set /goal: "all tasks in current phase done, or stop after 60 turns"
   Haiku evaluates the condition after each turn
3. Per task (PLAN → DELEGATE → ASSESS → CODIFY):
   PLAN:     Read task brief + dependency summaries from .claude/state/summaries/
             Verify DoR checklist is satisfied
   DELEGATE: Invoke engineer subagent (worktree isolation)
             - Write ALL failing tests first (TDD)
             - Implement until tests pass
             - /simplify + /code-review
             - Commit to feature branch
   ASSESS:   Invoke qa subagent (worktree isolation)
             - 10-category validation
             - Mutation testing (≥ 60%)
             - If FAIL: feed report back to engineer, retry (max 3)
   CODIFY:   Write .claude/state/summaries/TASK-NNN.md
             Update + commit .claude/state/progress.json
             Open PR
4. When all phase tasks complete:
   Stop hook fires → phase_gate() detects phase completion → re-prompts Claude
5. Claude presents: kanban summary, PR list, phase gate checklist
6. AskUserQuestion: Approve → next phase | Amend → describe changes | Reject → replan
7. If Approve: advance phase in progress.json, loop to next phase
```

### Overnight / unattended pattern

```
Push to backlog-ready branch (or daily cron)
→ /implement reads committed progress.json
→ Builds tasks autonomously
→ Commits progress.json after each task
→ Opens PRs for human review
Gate: PR approval = human advance signal
```

---

## 8. Hooks — what fires when and why

All hooks live in `.claude/scripts/hooks.py` (Python, module-dispatched). Each hook module
owns one logical concern.

### Active hooks

| Event | Matcher | Handler | Effect |
|---|---|---|---|
| PreToolUse | Edit\|Write | `check-no-secrets` | Blocks hardcoded secrets before write |
| PreToolUse | Edit\|Write + Bash | `check-uv-over-pip` | Blocks `pip install` (uv only) |
| PostToolUse | Edit\|Write | `mark-anatomy-stale` | Appends stale marker to area wiki page |
| PostToolUse | Edit\|Write | `commit-progress` | Auto-commits `.claude/state/progress.json` when changed |
| Notification | — | `notification` | Logs Claude messages to stderr |
| UserPromptSubmit | — | `user-prompt-submit` | Injects project memory context |
| Stop | — | `phase_gate` | Fires HITL gate when all phase tasks are done |
| Stop | — | `drift_check` | Context hygiene suggestions via local LLM (optional) |
| SubagentStop | — | `subagent_stop` | Injects task summary back to parent implement loop |
| PreCompact | — | `pre_compact` | Snapshot state before context compaction |
| SessionStart | — | `check_setup_status` | Warns if git hooks are not wired |
| SessionEnd | — | `session_end` | Flush pending state |

### Phase gate mechanics

The `phase_gate` function in `stop.py` reads `.claude/state/progress.json`. When all
tasks show `done` status:

```
Hook fires → writes HITL prompt to stderr → sys.exit(2) (Claude re-reads stdin)
Claude re-starts → formats kanban + phase gate checklist
AskUserQuestion: Approve / Amend / Reject
Approve → implement skill continues to next phase
```

The `stop_hook_active: true` payload guard prevents re-entry from `/goal`'s own Stop hook.

### Anatomy staleness tracking

The `mark_anatomy_stale` function in `wiki.py` runs on every Edit/Write. It:
1. Maps the edited file to its wiki area via `common.area_for_path()`
2. Only acts on files under tracked roots: `apps/`, `packages/`, `infra/`, `src/`, `lib/`
3. Appends `<!-- stale: <path> -->` to that area's `docs/wiki/<area>.md`
4. The pre-push git hook (`check-anatomy-fresh`) blocks if stale markers remain

---

## 9. OKF knowledge graph

The harness maintains an **Open Knowledge Format (OKF) v0.1** bundle spanning all docs.
This makes every spec, standard, and wiki page navigable as a connected knowledge graph.

### What the bundle contains

| Bundle path | Types included |
|---|---|
| `docs/specs/weave/engines/<entity>.md` | Brief, PRD, Roadmap, Tech Spec artifacts, ADRs, Task Briefs |
| `docs/standards/` | Coding Standards |
| `docs/wiki/` | Code Area anatomy pages |
| `docs/index.md` | Bundle root (OKF metadata only) |
| `docs/log.md` | Bundle change history |

### OKF conformance rules

Every non-reserved `.md` file must have:
- YAML frontmatter block (hard error if absent)
- Non-empty `type` field (hard error if absent)

Recommended fields: `title`, `description`, `tags`, `timestamp`.

Reserved files: `index.md` (no frontmatter in sub-dirs; only `okf_version` at root),
`log.md` (no frontmatter; ISO 8601 date headings only).

### Generating the graph

```bash
# Validate conformance (hard errors only)
uv run .claude/scripts/okf_validate.py docs/

# Validate with warnings as errors (CI mode)
uv run .claude/scripts/okf_validate.py docs/ --strict

# Render interactive graph
uv run .claude/scripts/okf_visualize.py docs/ -o docs/viz.html
open docs/viz.html
```

The visualizer renders:
- **Nodes** — one per OKF concept, coloured by `type`, sized by content length
- **Edges** — markdown cross-links between concept files
- **Panel** — click any node to see its full content, outgoing links, and "Cited by" backlinks

Cross-links between spec files use bundle-relative markdown links:
```markdown
[constitution-engine brief](engines/constitution-engine.md#brief)
```

### Nested sub-bundle note

`/anatomy` validates `docs/wiki/` as its own sub-bundle for code anatomy checks.
`/okf-validate` (and the unified graph) operates on the full `docs/` bundle.
The root `docs/index.md` has only `okf_version`; `docs/wiki/index.md` (when created by
`/anatomy`) is a non-root index within the unified bundle — treated as a navigation aid,
not an OKF concept.

---

## 10. Getting started — new feature or engine spec

**Run `/po` and answer the questions. That's it.**

More precisely:

```bash
# Step 1 — understand the problem space
/elicit             # choose 20Q for a new engine spec

# Step 2 — product spec
/po                 # when asked, say e.g. "constitution-engine"
                    # answer questions for brief, PRD, roadmap, epics
                    # each section shown before write; approve/amend/reject

# Step 3 — architecture (after PO artifacts are approved)
/architect          # generates stack → C4 → API → data model → task briefs
                    # each section shown before write

# Step 4 — implementation readiness check
/spec-review        # reads all specs; surfaces gaps before any code

# Step 5 — build
/implement          # dark factory loop; runs autonomously
                    # pauses at phase boundaries for your approval
```

### Resuming after a break

```bash
/status             # kanban: current phase, blocked tasks, done count
/implement          # picks up from .claude/state/progress.json
```

### Checking quality

```bash
/qa                 # validate current task against its brief
/code-review        # review current diff
/security-review    # security pass (run manually or auto at phase gate)
```

### Saving team knowledge

```bash
/remember <fact>    # saves to .claude/memory/MEMORY.md (committed, team-visible)
```

---

## 11. State files

| File | Written by | Read by | Purpose |
|---|---|---|---|
| `docs/specs/weave/engines/<entity>.md` | PO / Architect skills | All agents | Spec artifacts |
| `.claude/state/progress.json` | implement, progress.sh | All loops, routines, Stop hook | Task/phase state |
| `.claude/state/summaries/TASK-NNN.md` | Engineer subagent | Next task's PLAN step | Task context chain |
| `.claude/state/summaries/PHASE-N.md` | phase-gate skill | Human review, next phase | Phase completion record |
| `.claude/state/escalations/*.md` | Engineer (on ambiguity) | Human | Blocked task details |
| `.claude/memory/MEMORY.md` | project-memory skill | SessionStart | Team decisions, conventions |
| `ANATOMY.md` | /anatomy command | All agents | Per-area wiki page map |
| `docs/wiki/<area>.md` | /anatomy command | Agents, jcodemunch fallback | Code area anatomy |
| `docs/viz.html` | /okf-visualize | Humans (browser) | Interactive knowledge graph |

---

## 12. Model right-sizing

Skills declare their model tier. No guessing — the tier is fixed per artifact type.

| Task type | Model | Rationale |
|---|---|---|
| Elicitation / creative brief writing | High tier | Wide reasoning, novel framing |
| Architecture design, C4 diagrams | High tier | Spatial reasoning, tradeoff analysis |
| Task brief writing | High tier | Self-contained, must be complete |
| PRD stories, flows, OpenAPI | Mid tier | Structured but not exploratory |
| Data models, class diagrams | Mid tier | Precision over creativity |
| Stack confirmation, open dimensions | Mid tier (detect) + HITL | Validation tier; human confirms |
| DoR/DoD, YAML/config generation | Mid tier | Mechanical, template-following |
| Validation, lint checks | Mid tier | Deterministic tooling first; mid tier for judgement |
| Code implementation (engineer) | Mid tier | TDD iteration |
| Security review | High tier | High-stakes, adversarial |

---

## 13. Conventions

- **Spec location**: `docs/specs/weave/engines/<entity>.md`
- **Template location**: `.claude/spec-templates/<artifact>.md` (stays in `.claude/`)
- **Progress**: `.claude/state/progress.json` (always committed; routines depend on it)
- **Skills**: `.claude/skills/<skill-name>/SKILL.md`
- **Agents**: `.claude/agents/<persona>.md`
- **Hooks**: `.claude/scripts/hooks.py` + modules in `.claude/scripts/modules/`
- **No `pip install`**: uv only (enforced by hook — exits 2 on `pip` usage)
- **No hardcoded secrets**: AWS Secrets Manager only (enforced by hook)
- **Conventional commits**: `feat:`, `fix:`, `docs:`, `test:`, `chore:` (required by commitlint hook)

---

## 14. Key design decisions

| Decision | What | Reference |
|---|---|---|
| Skill granularity | Per artifact/template (not per persona, not per section); each skill maps 1:1 to a spec template — templates are the natural artifact seams | Harness redesign, 2026-06-25 |
| HITL gates | Spec = section-by-section; implement = phase-gated groups (PO-defined) | Harness redesign, 2026-06-25 |
| Loop mechanism | `/goal` CLI primitive (native built-in, v2.1.139+); Haiku-evaluated; `phase_gate()` Stop hook for HITL | Harness redesign, 2026-06-25 |
| Agent runtime | Anthropic Agent SDK (authoring) → AgentCore/Bedrock (GA only) | Harness redesign, 2026-06-25 |
| Triplestore | Oxigraph dev → Neptune/Fuseki eval prod (decision deferred to CE tech spec) | Harness redesign, 2026-06-25 |
| Spec location | `docs/specs/` (joined OKF bundle at `docs/`) rather than `.claude/specs/` | Pass 1 migration, 2026-06-29 |
| Architect artifact layout | Engine-lifetime `tech-spec/` + `decisions/` alongside milestone-scoped `m1/tasks/` (sibling `m2/`, `v1/`, `post-v1/`), replacing the flat `04-arch/` container | Layout restructure, 2026-07-02 |
