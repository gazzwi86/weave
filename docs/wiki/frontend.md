---
type: Code Area
title: frontend — Next.js 16 shell (health route only, pre-MVP scaffold)
description: Next.js 16 shell (health route only, pre-MVP scaffold)
tags: [typescript, nextjs, frontend, react]
timestamp: 2026-07-03T05:00:00Z
---

_Anatomy — regenerated by `/anatomy`. Last refresh: 2026-07-03._

## app/layout.tsx
Root layout — sets up Geist fonts and the HTML/body shell for the whole app.
- `RootLayout({ children })` — wraps every page, applies font variables and base layout classes

## app/page.tsx
Home page.
- `Home()` — renders the placeholder landing page ("Weave" heading)

## app/api/health/route.ts
Liveness endpoint (Next.js route handler, Node runtime).
- `GET()` — handles `GET /api/health`, returns `{ status, timestamp, version }`

## app/__tests__/page.test.tsx
Unit test for the home page.
- Tests `Home()` renders without error

## app/globals.css
Global Tailwind styles.

See also: [backend](backend.md)
