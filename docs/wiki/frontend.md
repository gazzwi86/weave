---
type: Code Area
title: frontend — Next.js SPA shell (auth, design system, protected routes)
description: Next.js App Router shell — next-auth v5 OIDC auth with silent refresh, shadcn/ui design-system components + Storybook, protected dashboard, security headers
tags: [typescript, nextjs, frontend, react, next-auth, shadcn]
timestamp: 2026-07-16T00:00:00Z
---

_Anatomy — regenerated by `/anatomy`. Last refresh: 2026-07-16 (full)._

## auth.ts

next-auth v5 (Auth.js) config — generic OIDC provider pointed at `OIDC_ISSUER_URL` (mock locally, Cognito later by env change only). JWT callback handles refresh-token rotation; `signIn`/`signOut` exported.

## middleware.ts

Route protection — unauthenticated access to protected paths 302s to `/auth/login?return_to=<path>` (known gap: query string of the original URL is dropped; locked by an E2E test).

## app/

- `layout.tsx` — root layout (fonts, HTML shell)
- `page.tsx` — public landing page
- `auth/login/page.tsx` — sign-in page ("Sign in with Weave" → OIDC flow)
- `dashboard/page.tsx` — protected page; shows `principal_iri` from backend-verified session
- `api/auth/[...nextauth]/route.ts` — next-auth handler
- `api/health/route.ts` — `GET /api/health` liveness `{status, timestamp, version}`
- `globals.css` — Tailwind + design tokens (per `docs/standards/design/tokens.md`; type-scale partially populated — see qa-cross-task-findings)

## components/ui/

shadcn/ui baseline components, each with a Storybook story and axe a11y coverage (`ui.a11y.test.tsx`).

- `button.tsx` / `input.tsx` / `badge.tsx` / `card.tsx` (+ `*.stories.tsx`)

## lib/

- `auth/refresh.ts` — refresh-token grant call to backend (+ unit test)
- `rate-limit.ts` — in-memory fixed-window limiter for auth routes (+ unit test)
- `utils.ts` — `cn()` class-merge helper

## types/next-auth.d.ts

Module augmentation for session/JWT shapes (gotcha: augment `@auth/core/jwt`, the `next-auth/jwt` re-export does not merge).

## tests/e2e/auth.spec.ts

Playwright: redirect → mock-OIDC login → return-to-dashboard round trip; query-string return_to behaviour. `auth.ts` fails closed in production when OIDC_CLIENT_SECRET is unset.

## .storybook/

Storybook config (`main.ts`, `preview.tsx`) with addon-vitest browser tests.

## components/shell/ (PLAT-TASK-005)

Global app chrome — persistent nav, Cmd+K search, help.

- `nav-items.ts` — `NAV_ITEMS`, the single source for the 7 product areas (nav + any future links)
- `nav.tsx` — top nav; active area `aria-current="page"`; `prefetch={false}` on not-yet-built routes
- `command-palette.tsx` — cmdk Cmd+K/Ctrl+K search palette (focus trap, Escape dismiss)
- `use-entity-search.ts` — debounced tenant-scoped `/api/search` hook (2+ chars)
- `search-result-item.tsx` — result row → client-nav to `/ce/resource?iri=`
- `help-launcher.tsx` — `?` icon → radix dialog help panel (no navigation)
- `app-shell.tsx` — composes nav + palette + help; wired into root layout

## components/dashboard/dashboard-placeholder.tsx

M1 dashboard empty state (headline/body/footer, grid layout, axe-clean, zero CE calls). Real `<h1>`; footer uses `--color-text-muted` (WCAG AA).

## app/api/search/route.ts

Frontend proxy → backend `GET /api/search`.

## app/robots.ts

Next metadata robots route (public via middleware PUBLIC_PATHS).

## scripts/serve-prod.sh

Production build server (`next build && next start`, AUTH_TRUST_HOST) — what ui_verify measures.

## app/compliance/

Audit-compliance page — month-over-month trends + SHACL conformance hub.

- `page.tsx` — summary card (chain badge, per-category counts with ▲/▼ deltas vs previous
  month) + "Model conformance" card (validated writes / SHACL rejections, link to `/ce/types`)
- `use-compliance.ts` — fetches current + previous month (`?period=YYYY-MM`) in one mount
  effect (month strings computed post-mount only — SSR hydration rule); previous-month
  failure degrades to no deltas

## app/api/audit/compliance/route.ts

Proxy → backend `GET /api/audit/compliance`; zod-validates and forwards the optional
`period` query param, attaches the session bearer token.

## app/api/proxy/node-kinds/route.ts

Proxy → CE-READ-1 `GET /api/ontology/types`, adapting kinds into the explorer palette —
known BPMO kinds map to `var(--color-kind-<kind>)` design tokens, unknown kinds fall back
to `var(--color-kind-fallback)`.

## components/explorer/explorer-interactions.tsx

Composes spotlight/search/domain-focus/expand-collapse onto the renderer-adapter seam.
`useFocusParam` handles `/explorer?focus=<iri>` deep links (chat entity links) — polls
`getNodeData` until the async graph load lands (250ms × 40), then centres + spotlights;
gives up silently if the node never appears.

## tests/e2e/

Playwright suite against the real dev stack — auth round trip, IA skeleton, CE query,
audit dashboard/logs, workspaces provisioning (idempotent fixed `e2e-sandbox` slug — no
delete endpoint, so unique-per-run slugs would leak junk rows), models settings, build
request, workspace switcher.

## app/ routes (v1 surfaces)

Each is an App-Router page (auth-gated → 307 to `/auth/login` when signed out):

- `app/explorer/` — Graph Explorer (Cytoscape canvas, side-panel property edit, draw-edge / quick-add).
- `app/ce/` — Constitution-engine surfaces (`CePage`): ontology types, instances, glossary, rules/validate.
- `app/build/` — Build registry (`BuildRegistryPage`) + request form.
- `app/dashboard/` — role dashboard + widget grid.
- `app/events/` — Events & Actions (`EventsPage`).
- `app/help/` — help launcher + training library + What's-new (`HelpPage`).
- `app/role-home/` — role-home guidance + completeness-question beacon.
- `app/notifications/` — notification centre.
- `app/billing/` — billing / metering (`BillingPage`).
- `app/audit/` — audit dashboard + logs (`AuditDashboardPage`).
- `app/settings/` — models + project settings (`SettingsPage`).

## components/ groups

Design-system-layered (atoms → `ui/`, then `molecules/`, `organisms/`, `templates/`):
`explorer/` (canvas + interactions), `dashboard/` (widgets), `onboarding/` (beacons, tours,
checklist, hints host), `shell/` (nav + app frame), `marketing/` (landing), `tooling/`.

## lib/ groups

Per-surface API clients + helpers: `lib/explorer/` (`commitOp` / `commitUpdate` / `commitDelete`,
saved views, coverage gaps), `lib/onboarding/` (anchors, upstream-fetch proxy, tour queries),
`lib/{auth,build,dashboard,glossary,instances}/`.

Related: [backend](backend.md), [shared](shared.md)
