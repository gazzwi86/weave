---
type: Code Area
title: backend — FastAPI platform API (health endpoint only, pre-MVP scaffold)
description: FastAPI platform API (health endpoint only, pre-MVP scaffold)
tags: [python, fastapi, backend, api]
timestamp: 2026-07-03T05:00:00Z
---

_Anatomy — regenerated by `/anatomy`. Last refresh: 2026-07-03._

## src/weave_backend/__init__.py
FastAPI app factory — builds the `app` instance and wires in routers.
- `app` — the FastAPI application instance, titled "Weave Platform API"
- `main() -> None` — dev entrypoint, runs the app via uvicorn on port 8000

## src/weave_backend/routers/health.py
Liveness endpoint router.
- `router` — APIRouter tagged "health"
- `get_health() -> HealthResponse` — handles `GET /health`, returns status/timestamp/version

## src/weave_backend/schemas/health.py
Response schema for the health endpoint.
- `HealthResponse` — Pydantic model: `status` (default "ok"), `timestamp` (UTC, defaults to now), `version`

## tests/unit/test_health.py
Unit test for the health endpoint.
- `test_health_returns_ok_status()` — asserts `GET /health` returns 200 with `status`, `timestamp`, `version` present

See also: [frontend](frontend.md)
