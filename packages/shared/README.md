# packages/shared

Placeholder for code shared between `packages/backend` and `packages/frontend`
(e.g. generated OpenAPI types, shared constants). Empty until a task actually
needs to share something — see `docs/specs/weave/engines/weave-platform/m1/tasks/TASK-001.md`
for the scaffold this directory is part of.
