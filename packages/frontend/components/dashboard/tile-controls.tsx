// ponytail: stub so `tsc` passes on the RED commit -- filled in on GREEN.
export function TileControls(_props: {
  title: string;
  onPin?: () => void;
  onUnpin?: () => void;
  onMoveUp?: () => void;
  onMoveDown?: () => void;
  onPublish?: () => void;
  showPin: boolean;
}) {
  return null;
}
