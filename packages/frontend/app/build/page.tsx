import { PlaceholderPage } from "@/components/placeholder-page";

export default function BuildPage() {
  return (
    <PlaceholderPage
      title="Build"
      tag="v1.0"
      description="Request an application generated from your company graph. The M1 dark-factory loop runs headless; the Request application form lands this pass, and the PM surfaces (projects registry, dashboards, kanban, task briefs) are delivered in phase v1.0."
    />
  );
}
