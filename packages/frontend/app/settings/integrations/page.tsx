import { PlaceholderPage } from "@/components/placeholder-page";

export default function IntegrationsPage() {
  return (
    <PlaceholderPage
      title="Integrations"
      tag="v1.0"
      description="Connect managed data sources -- Snowflake, Databricks, S3, Azure Data Lake, Atlassian, ServiceNow, Slack -- so the graph ingests directly from the systems you already run. Ships in v1.0."
    />
  );
}
