import { PlaceholderPage } from "@/components/placeholder-page";

export default function SettingsModelsPage() {
  return (
    <PlaceholderPage
      title="Models & AI"
      tag="m1"
      description="Model routing across the two tiers (fable for judgement-heavy work, sonnet for volume work) and per-workspace budget caps. The routing and budget backends are done; this thin config UI lands this pass."
    />
  );
}
