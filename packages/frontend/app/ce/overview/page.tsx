import { PlaceholderPage } from "@/components/placeholder-page";

export default function CeOverviewPage() {
  return (
    <PlaceholderPage
      title="Constitution overview"
      tag="m1"
      description="Counts by kind, draft vs published state, and recent changes — a thin read over CE-READ-1. Full model-health tiles arrive with CE-METRICS-1 in M2."
    />
  );
}
