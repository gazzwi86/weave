import { PlaceholderPage } from "@/components/placeholder-page";

export default function CeVersionsPage() {
  return (
    <PlaceholderPage
      title="Versions"
      tag="m1"
      description="Draft-to-published lifecycle, semantic diffs (CE-DIFF-1), and the PROV-O change log. The versioning APIs are done; this UI lands this pass."
    />
  );
}
