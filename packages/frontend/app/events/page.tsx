import { PlaceholderPage } from "@/components/placeholder-page";

export default function EventsPage() {
  return (
    <PlaceholderPage
      title="Events"
      tag="post-v1"
      description="Create and monitor event-to-action automations: graph-change triggers, schedules, webhooks, and per-run metering. The whole Events & Actions engine is delivered post-v1."
    />
  );
}
