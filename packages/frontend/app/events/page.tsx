import { PlaceholderPage } from "@/components/placeholder-page";

export default function EventsPage() {
  return (
    <PlaceholderPage
      title="Events"
      description="Create and monitor event-to-action automations: graph-change triggers, schedules, webhooks, and per-run metering."
    />
  );
}
