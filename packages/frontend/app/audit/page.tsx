import { PlaceholderPage } from "@/components/placeholder-page";

export default function AuditDashboardPage() {
  return (
    <PlaceholderPage
      title="Audit trail"
      tag="m1"
      description="Metrics over the immutable, hash-chained audit log: event volume by engine and actor, error and agent-failure rates, chain-verified badge. The compliance summary is live now under Audit trail → Compliance."
    />
  );
}
