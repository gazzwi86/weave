// ponytail: stub so `tsc` passes on the RED commit -- filled in on GREEN.
export function formatKpiValue(_value: unknown): { display: string; title?: string } {
  throw new Error("not implemented");
}
